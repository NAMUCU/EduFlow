# ============================================
# EduFlow 환경 변수 설정
# ============================================
# 이 파일을 복사하여 .env.local 파일을 만드세요:
# cp .env.example .env.local
#
# 중요: .env.local 파일은 절대 Git에 커밋하지 마세요!
# (.gitignore에 이미 포함되어 있습니다)
# ============================================

# ============================================
# Supabase 설정 (필수)
# ============================================
# Supabase 프로젝트 URL
# Supabase 대시보드 > Settings > API에서 확인
# 형식: https://[프로젝트ID].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase 익명 키 (anon public key)
# Supabase 대시보드 > Settings > API에서 확인
# 이 키는 클라이언트에서 사용되며, Row Level Security로 보호됩니다
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase 서비스 역할 키 (service_role key)
# 주의: 이 키는 RLS를 우회합니다. 서버에서만 사용하세요!
# 절대 클라이언트에 노출하지 마세요!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# AI API 설정
# ============================================
# Gemini API 키 (문제 생성용)
# Google AI Studio에서 발급: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# OpenAI API 키 (문제 검수, RAG용)
# OpenAI 대시보드에서 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Claude API 키 (문제 검수용)
# Anthropic 콘솔에서 발급: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# ============================================
# OCR 설정 (멀티모델 지원)
# ============================================
# OCR 기능은 다음 4가지 AI 모델을 지원합니다.
# 최소 1개 이상의 API 키를 설정하면 해당 모델을 사용할 수 있습니다.
# 모든 키가 없으면 Mock 모드로 동작합니다 (테스트용 가짜 데이터 반환)

# 1. Claude (Anthropic) - 권장
# 지원 모델: Claude Haiku 3.5, Claude Sonnet 3.5, Claude Sonnet 4.5
# Anthropic 콘솔에서 발급: https://console.anthropic.com/
# (위의 ANTHROPIC_API_KEY를 사용합니다)

# 2. Gemini (Google AI)
# 지원 모델: Gemini 1.5 Flash, Gemini 1.5 Pro, Gemini 2.0 Flash
# (위의 GEMINI_API_KEY를 사용합니다)

# 3. OpenAI
# 지원 모델: GPT-4o, GPT-4o Mini
# (위의 OPENAI_API_KEY를 사용합니다)

# 4. Google Cloud Vision API (전통적 OCR)
# Google Cloud Console에서 발급: https://console.cloud.google.com/apis/credentials
GOOGLE_CLOUD_VISION_API_KEY=your-google-cloud-vision-api-key

# 또는 서비스 계정 키 JSON 파일 경로
# GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# ============================================
# PDF 변환 설정
# ============================================
# PDF.co API 키
# https://pdf.co에서 발급
PDF_CO_API_KEY=your-pdf-co-api-key

# ============================================
# SMS 문자 발송 설정
# ============================================
# SMS 기능은 다음 3가지 서비스를 지원합니다.
# 최소 1개 이상의 API 키를 설정하면 해당 서비스를 사용할 수 있습니다.
# 모든 키가 없으면 Mock 모드로 동작합니다 (실제 발송 없이 테스트)

# SMS 제공자 선택 (선택사항, 미설정시 자동 감지)
# 옵션: tally, coolsms, nhncloud, mock
# SMS_PROVIDER=mock

# 1. Tally SMS
# Tally 대시보드에서 발급
TALLY_API_KEY=your-tally-api-key
TALLY_SENDER_PHONE=01012345678

# 2. CoolSMS (추천 - 한국 서비스)
# https://coolsms.co.kr에서 발급
# COOLSMS_API_KEY=your-coolsms-api-key
# COOLSMS_API_SECRET=your-coolsms-api-secret
# COOLSMS_SENDER_PHONE=01012345678

# 3. NHN Cloud SMS
# https://www.nhncloud.com/kr/service/notification/sms에서 발급
# NHN_CLOUD_APP_KEY=your-nhn-cloud-app-key
# NHN_CLOUD_SECRET_KEY=your-nhn-cloud-secret-key
# NHN_CLOUD_SENDER_PHONE=01012345678

# ============================================
# 카카오 알림톡 설정
# ============================================
# 카카오 비즈니스 알림톡을 사용하려면 다음 설정이 필요합니다.
# SMS보다 저렴하고 수신율이 높습니다.

# 카카오 API 키 (REST API Key)
# https://developers.kakao.com에서 앱 생성 후 발급
KAKAO_API_KEY=your-kakao-api-key

# 알림톡 발신 프로필 키 (카카오 비즈니스에서 발급)
# https://business.kakao.com에서 채널 등록 후 발급
KAKAO_SENDER_KEY=your-kakao-sender-key

# ============================================
# 앱 설정
# ============================================
# 앱이 실행되는 URL (개발 환경)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 앱 환경 (development, staging, production)
NODE_ENV=development
